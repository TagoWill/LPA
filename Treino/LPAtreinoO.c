#include <stdio.h>
#include <stdlib.h>

void verificaCoisas(int posi, int ){
    
}

int main() {
    int n, m, i;
    
    scanf("%d %d", &n, &m);
    int ligacoes[n][2];
    int usado[n];
    
    for(i=0;i<m;i++){
        scanf("%d %d", &ligacoes[i][0], &ligacoes[i][1]);
    }

    return 0;
}
